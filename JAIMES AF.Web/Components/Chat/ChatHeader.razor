<div class="chat-header">
    <span class="chat-header-name">@Name</span>
</div>

@code {
    /// <summary>
    /// The name to display in the header (e.g., "Player", "Game Master", "System")
    /// </summary>
    [Parameter]
    public string? Name { get; set; }
}
